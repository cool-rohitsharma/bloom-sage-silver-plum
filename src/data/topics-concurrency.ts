import type { Topic } from "./types";

export const concurrencyTopics: Topic[] = [
  {
    id: "threads-basics",
    chapterId: "concurrency",
    title: "Process vs thread, lifecycle",
    blurb: "A thread is an execution path inside a process. Threads share the heap; each has a stack. Lifecycle is a state machine, not a suggestion.",
    keywords: ["NEW", "RUNNABLE", "BLOCKED", "WAITING", "TIMED_WAITING", "TERMINATED"],
    source: "merged",
    level: "core",
    blocks: [
      {
        kind: "table",
        headers: ["Process", "Thread"],
        rows: [
          ["Heavy, own address space", "Light, shares process memory"],
          ["Slow to create/kill", "Faster to create/kill"],
          ["Isolation", "Shared heap → races"],
        ],
      },
      { kind: "idea", text: "Every Java program starts with the main thread. Extra threads are either a Thread subclass or a Runnable/Callable handed to an executor. The scheduler, not you, picks who runs — behaviour is not portable timing." },
      { kind: "bullets", title: "States (Java)", items: ["NEW — constructed, not started.", "RUNNABLE — runnable or actually running (JVM does not expose RUNNING).", "BLOCKED — waiting to enter a synchronized monitor.", "WAITING — wait() / join() / park() without timeout.", "TIMED_WAITING — sleep, wait(t), join(t).", "TERMINATED — run finished. You cannot restart it."] },
      { kind: "trap", text: "thread.start() twice → IllegalThreadStateException. thread.run() does not start a thread — it runs on the caller. If you override start() and forget super.start(), run() never happens on a new thread." },
    ],
  },
  {
    id: "runnable-callable",
    chapterId: "concurrency",
    title: "Runnable vs Callable, start vs run",
    blurb: "Runnable is fire-and-forget. Callable returns a value and may throw checked exceptions. Prefer implementing Runnable so you can still extend another class.",
    keywords: ["run", "call", "Future", "start"],
    source: "merged",
    level: "core",
    blocks: [
      {
        kind: "table",
        headers: ["", "Runnable", "Callable<V>"],
        rows: [
          ["Method", "void run()", "V call() throws Exception"],
          ["Result", "none", "Future<V>"],
          ["Package", "java.lang", "java.util.concurrent"],
        ],
      },
      { kind: "why", text: "ExecutorService.submit(callable) gives you a Future. That is how you join on a result without rolling your own thread list." },
    ],
  },
  {
    id: "sync-wait-notify",
    chapterId: "concurrency",
    title: "synchronized, wait, notify",
    blurb: "synchronized is an intrinsic lock plus a memory barrier. wait/notify are the condition variables on that same monitor.",
    keywords: ["monitor", "mutual exclusion", "visibility", "wait", "notify"],
    source: "merged",
    level: "core",
    blocks: [
      { kind: "idea", text: "Each object has a monitor. synchronized(obj) lets only one thread into the critical section and makes writes visible to the next acquirer. Keep the section small. Always lock in a global order to avoid deadlock." },
      {
        kind: "table",
        headers: ["wait()", "sleep()"],
        rows: [
          ["Instance method on Object", "Static on Thread"],
          ["Releases the monitor", "Keeps the lock"],
          ["Must be inside synchronized", "No such rule"],
          ["Woken by notify/notifyAll or timeout", "Woken by time or interrupt"],
        ],
      },
      { kind: "bullets", title: "Also know", items: ["yield — hint to the scheduler, no lock release, rarely useful.", "join — wait for another thread to terminate.", "Always wait() in a while (condition) loop — spurious wakeups are real.", "notify wakes one; notifyAll wakes all. Prefer notifyAll unless you can prove only one waiter can proceed."] },
    ],
  },
  {
    id: "deadlock",
    chapterId: "concurrency",
    title: "Deadlock, livelock, starvation",
    blurb: "Deadlock: everyone holds something and waits for something else. Break it with lock ordering, tryLock, or fewer locks.",
    keywords: ["deadlock", "livelock", "starvation", "lock order"],
    source: "merged",
    level: "core",
    blocks: [
      { kind: "idea", text: "Four Coffman conditions: mutual exclusion, hold-and-wait, no preemption, circular wait. Remove any one. In Java interviews, lock ordering is the answer they want." },
      { kind: "bullets", items: ["Avoid nested locks when you can.", "Use tryLock with timeout.", "Do not hold a lock while calling unknown/foreign code.", "Starvation: a thread never gets the lock (unfairness, high priority others).", "Livelock: threads keep yielding to each other and make no progress."] },
      { kind: "project", text: "Transfer money: always lock accountMin(id) then accountMax(id). Never lock A then B on one path and B then A on another." },
    ],
  },
  {
    id: "executors",
    chapterId: "concurrency",
    title: "ExecutorService and thread pools",
    blurb: "Do not new Thread per task. Size the pool, bound the queue, shut it down, and know the rejection policy.",
    keywords: ["fixed", "cached", "scheduled", "rejection", "shutdown"],
    source: "merged",
    level: "core",
    blocks: [
      { kind: "bullets", items: ["newFixedThreadPool(n) — n workers, unbounded LinkedBlockingQueue. Risk: queue grows forever.", "newCachedThreadPool — 0-core, SynchronousQueue, creates threads as needed. Can explode under load.", "newSingleThreadExecutor — ordered, one worker.", "newScheduledThreadPool — delayed / periodic.", "ThreadPoolExecutor(core, max, keepAlive, unit, queue, factory, handler) is the real constructor."] },
      { kind: "why", text: "Reuse threads, cap concurrency, get Futures. Always shutdown() or shutdownNow() in application lifecycle (Spring: @PreDestroy or a managed TaskExecutor)." },
      { kind: "trap", text: "Unbounded queues mean max pool size is never reached (fixed pool with LinkedBlockingQueue). Cached pools can create thousands of threads. Rejection policies: Abort, CallerRuns, Discard, DiscardOldest — pick on purpose." },
    ],
  },
  {
    id: "completable-future",
    chapterId: "concurrency",
    title: "CompletableFuture",
    blurb: "Compose async work without callback pyramids. Be explicit about which executor runs the next stage.",
    keywords: ["thenApply", "thenCompose", "allOf", "exceptionally"],
    source: "ultimate",
    level: "deep",
    blocks: [
      { kind: "bullets", items: ["thenApply — map the value.", "thenCompose — flatMap async.", "thenAccept / thenRun — side effects.", "thenCombine / allOf / anyOf — join independent work.", "exceptionally / handle / whenComplete — recovery."] },
      { kind: "trap", text: "thenApply uses the completing thread, which may be the common ForkJoinPool. Blocking I/O there starves everyone. Use thenApplyAsync(fn, myExecutor) for blocking stages." },
      { kind: "project", text: "Fan-out: fetch customer, fetch products, thenCombine into a page DTO. Timeout with orTimeout / completeOnTimeout in newer JDKs." },
    ],
  },
  {
    id: "advanced-concurrency",
    chapterId: "concurrency",
    title: "Locks, atomics, ThreadLocal, Fork/Join",
    blurb: "ReentrantLock for tryLock and fairness. ReadWriteLock when reads dominate. Atomics for single variables. ThreadLocal must be removed on pooled threads.",
    keywords: ["ReentrantLock", "ReadWriteLock", "CAS", "ThreadLocal", "ForkJoin"],
    source: "ultimate",
    level: "deep",
    blocks: [
      { kind: "bullets", items: ["ReentrantLock — lockInterruptibly, tryLock, fair option, multiple conditions.", "ReadWriteLock — many readers or one writer.", "AtomicInteger / LongAdder — CAS, no blocking. LongAdder better under high write contention.", "ForkJoinPool + RecursiveTask — CPU-bound divide and conquer. The common pool is what parallel streams use.", "ThreadLocal — per-thread value. In Tomcat/worker pools, always remove() in a finally, or the next request inherits MDC/user."] },
      { kind: "trap", text: "A ThreadLocal leak is a classloader leak in many app servers. Memory goes up after each hot reload. Clean it." },
    ],
  },
  {
    id: "jmm-volatile",
    chapterId: "concurrency",
    title: "volatile, happens-before, JMM",
    blurb: "Filled gap. volatile is visibility and ordering, not atomicity of ++. synchronized gives both exclusion and visibility.",
    keywords: ["volatile", "happens-before", "visibility", "atomic"],
    source: "filled",
    level: "trap",
    blocks: [
      { kind: "idea", text: "The Java Memory Model says when a write in thread A is visible to thread B. A happens-before B if, among other things: unlock happens-before later lock on the same monitor; write to volatile happens-before later read of that volatile; start happens-before the thread body; the body happens-before join returns." },
      { kind: "why", text: "Without this, a thread can spin forever on a cached 'running = true'. volatile boolean running is the stop-flag pattern." },
      { kind: "trap", text: "volatile int n; n++ is still a race — read, add, write. Use AtomicInteger. Double-checked locking for singletons needs volatile on the instance field." },
    ],
  },
  {
    id: "synchronizers",
    chapterId: "concurrency",
    title: "CountDownLatch, CyclicBarrier, Semaphore, Phaser",
    blurb: "Filled gap. Latch is one-shot. Barrier resets. Semaphore is permits. These show up in 'coordinate N workers' questions.",
    keywords: ["CountDownLatch", "CyclicBarrier", "Semaphore", "Phaser"],
    source: "filled",
    level: "deep",
    blocks: [
      { kind: "bullets", items: ["CountDownLatch(n) — wait until n countDowns. Cannot reset.", "CyclicBarrier(n) — n threads wait for each other, then continue, reusable.", "Semaphore(n) — n permits. Good as a simple bulkhead around a scarce resource.", "Phaser — dynamic party count, multi-phase."] },
      { kind: "project", text: "Start a test only after 8 stub servers are up: latch. Limit 20 concurrent downstream calls: semaphore around the WebClient call." },
    ],
  },
  {
    id: "daemon-priority",
    chapterId: "concurrency",
    title: "Daemon threads and priority",
    blurb: "Daemon threads die when the last user thread dies. Priority is 1–10 and is a hint, not a promise.",
    keywords: ["daemon", "priority", "JVM exit"],
    source: "cheatcode",
    level: "core",
    blocks: [
      { kind: "idea", text: "GC is a daemon. Your thread pool workers usually should not be daemons in a server — the JVM would exit under the HTTP layer if main returns. In a CLI, daemon helpers are fine." },
      { kind: "trap", text: "Priority is not portable. Never use it to fix a race." },
    ],
  },
];

export const jvmTopics: Topic[] = [
  {
    id: "jvm-architecture",
    chapterId: "jvm",
    title: "JDK, JRE, JVM, class loading",
    blurb: "javac writes bytecode. The JVM loads, verifies, interprets, and JIT-compiles it. JDK = JRE + tools.",
    keywords: ["bytecode", "classloader", "JIT", "interpreter", "JRE"],
    source: "merged",
    level: "core",
    blocks: [
      { kind: "idea", text: "Source .java → javac → .class bytecode → class loader → bytecode verifier → interpreter + JIT → runtime. WORA is this pipeline." },
      { kind: "bullets", title: "JDK vs JRE vs JVM", items: ["JDK — compiler, javadoc, jar, jlink, plus the JRE.", "JRE — JVM + core libs, enough to run.", "JVM — executes bytecode."] },
      { kind: "bullets", title: "Runtime data areas", items: ["Heap — objects.", "Per-thread Java stacks — frames, locals, return addresses.", "PC register per thread.", "Metaspace (native) — class metadata. Replaced PermGen in Java 8.", "Code cache — JIT compiled native code."] },
      { kind: "idea", text: "Class loaders: bootstrap (rt / java.base) → platform → application. Delegation is parent-first by default. A class is defined by its name plus its loader — that is how container isolation works." },
    ],
  },
  {
    id: "memory-layout",
    chapterId: "jvm",
    title: "Where things live",
    blurb: "Locals and refs on the stack. Objects on the heap. static on the class (metaspace + heap for the static fields' objects). String pool on the heap.",
    keywords: ["stack", "heap", "metaspace", "string pool", "frame"],
    source: "cheatcode",
    level: "core",
    blocks: [
      { kind: "bullets", items: ["Method call → new stack frame. Return → frame popped.", "Local primitives and references live in the frame.", "Objects (and their instance fields) live on the heap.", "static data is per-class, not per-instance.", "String literals are interned in the heap pool."] },
      { kind: "trap", text: "A stack overflow is too-deep recursion or a giant local array. An OutOfMemoryError: Java heap space is the object graph. OutOfMemoryError: Metaspace is classloader leaks (often undeployed apps + static caches)." },
    ],
  },
  {
    id: "gc",
    chapterId: "jvm",
    title: "Garbage collection",
    blurb: "Objects die when unreachable from GC roots. Generational heaps: young (Eden + survivors) and old. Collectors are a latency vs throughput choice.",
    keywords: ["Eden", "Survivor", "Old", "G1", "ZGC", "roots"],
    source: "merged",
    level: "deep",
    blocks: [
      { kind: "idea", text: "Roots: stack refs, static fields, JNI. Unreachable objects are collectable. Young objects die in minor collections. Long-lived objects are promoted. Major/full collection vocabulary depends on the collector." },
      { kind: "bullets", title: "Collectors", items: ["Serial — single thread, small heaps, client.", "Parallel — throughput, batch.", "G1 — default on modern JDKs, region-based, predictable pauses.", "ZGC / Shenandoah — ultra-low pause, large heaps.", "Pick from latency, throughput, heap size, not from blog habit."] },
      { kind: "idea", text: "System.gc() is a hint. Do not call it in a tight loop. finalize() is deprecated; use Cleaner or try-with-resources." },
    ],
  },
  {
    id: "jvm-tuning",
    chapterId: "jvm",
    title: "Tuning workflow",
    blurb: "Measure, identify the bottleneck, inspect artifacts, change one thing, re-test. Flags without evidence are theatre.",
    keywords: ["-Xmx", "G1", "JFR", "allocation rate"],
    source: "ultimate",
    level: "deep",
    blocks: [
      { kind: "bullets", title: "Order", items: ["Measure latency, throughput, CPU, allocation rate, pause times.", "Is it CPU, memory, I/O, locks, or the database?", "GC logs, thread dumps, heap dumps, JFR.", "Tune one knob.", "Re-test under realistic load."] },
      { kind: "bullets", title: "Tools", items: ["-Xms / -Xmx, -XX:+UseG1GC", "JFR + Java Mission Control", "jcmd, jstack, jmap", "JVisualVM, async-profiler, JProfiler", "Eclipse MAT for retained set", "JMH for microbenchmarks — and you will write them wrong the first time."] },
    ],
  },
  {
    id: "references",
    chapterId: "jvm",
    title: "Strong, soft, weak, phantom",
    blurb: "Filled gap. WeakHashMap keys vanish when only weakly reachable. Soft is for caches that should yield under memory pressure. Phantom is for cleanup after finalize's era.",
    keywords: ["WeakReference", "SoftReference", "PhantomReference", "WeakHashMap"],
    source: "filled",
    level: "deep",
    blocks: [
      { kind: "idea", text: "Strong — ordinary field. Soft — collected when the heap is desperate (used for image caches). Weak — collected at next GC once only weakly reachable (canonical maps, listeners). Phantom — enqueued after the object is phantom-reachable, used with a ReferenceQueue for post-mortem cleanup." },
      { kind: "trap", text: "WeakHashMap is identity-ish on keys and does not magically make values weak. If the value points back to the key, you leak." },
    ],
  },
];
